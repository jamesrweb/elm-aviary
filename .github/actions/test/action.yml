name: Test

description: Run tests

runs:
  using: composite
  steps:
    - uses: actions/checkout@v6
    - uses: pnpm/action-setup@v5
    - uses: actions/setup-node@v6
      with:
        node-version: lts/*
        cache: pnpm
    - run: pnpm install
      shell: bash
    - run: pnpm test
      shell: bash
