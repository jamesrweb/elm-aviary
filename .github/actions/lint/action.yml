name: Lint

description: Run linting

runs:
  using: composite
  steps:
    - uses: pnpm/action-setup@v5
    - uses: actions/setup-node@v6
      with:
        node-version: lts/*
        cache: pnpm
    - run: pnpm install
      shell: bash
    - run: pnpm run lint
      shell: bash
